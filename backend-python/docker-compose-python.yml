version: '3.4'
services:
  meteo.backend.python:
    container_name: meteo.backend.python
    image: meteocr.azurecr.io/meteo.backend.python:%IMAGE_TAG%
    restart: always
    ports:
      - 5000:5000
    networks:
      - fiek-met

networks:
  fiek-met:
    external: true